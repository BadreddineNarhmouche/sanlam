type Messages = Record<string, string>;
export declare const formatTranslationMessages: (locale: string, messages: Messages) => Messages;
export declare const translationMessages: {
    fr: Messages;
};
export {};
