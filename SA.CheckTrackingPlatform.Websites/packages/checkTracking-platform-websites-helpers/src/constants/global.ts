import {type IMap} from '..';

export const DEFAULT_LANGUAGE = 'fr';

export const GeneralConstants: IMap = {
  starString: '*',
  empty: '',
  whiteSpace: ' ',
  openingParenthesis: '(',
  closingParenthesis: ')',
};

export const PAGINATION = {
  PAGE_SIZE: 10,
};
